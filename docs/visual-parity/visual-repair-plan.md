# iOS 视觉修复计划 · 2026-09-26

## 本轮保留的证据

`apps/ios/.specdrive/parity-20260926` 保持原始运行状态、次数、日志和结果，不修改或清零。
第三次运行已通过当前源码的预构建及 9 条真实 iPhone 17e 操作测试。完整 53 状态已采集 183 张原始图片，并完成三个独立分组审查（按状态去重后实看 97 张附件（跨状态去重 91 张），86 张同状态字节重复逐项登记）。
独立审图确认星球整颗缺失已修复；同时确认下列视觉问题和采集缺口仍存在。功能测试通过不能关闭它们。

现有配置每项检查最多执行 3 次，成功执行也计数。预构建、预检等已达到总执行次数上限。
下一阶段不能伪称是旧运行的第四次成功，也不能改旧状态或调整其预算。

## 有明确证据的修复范围

1. **卡片模板按钮错位**：当前右侧按钮侵入模板计数行。将完整卡片绘图子树移出按钮布局，
   使用缓存的固定比例缩略图片；用显式等宽、同顶线布局放置两按钮。保留原图、文案、选择行为和模板持久化。
2. **最大辅助字号的光标可见性**：当前多行输入框超出键盘上方可用空间，光标落在保存栏后。
   仅将此刻输入换成随可用空间调整的原生多行编辑视口，保持系统字号、草稿绑定及默认视觉；
   长文在编辑框内滚动，聚焦或键盘大小变化时将外层编辑区滚入可见范围。

3. **卡片主预览布局泄漏**：复用现有 UIKit 画图结果作为有界缓存图片显示，主预览保持原版比例；缩略图固定尺寸。修复纸质卡片底栏缺失；验证两按钮顶部、宽度、高度、图像边界及标题间距，不能只比较已经相同的 AX 顶部。
4. **辅助字号阅读**：首页品牌与导航分行、聊天降级提示与设置动作纵排、账户标签和值纵排、完整显示时间日期；详情情绪摘要去重。保持系统大字号，不缩小正文以躲避问题。
5. **真实错误状态和安全区域**：修复登录错误夹具被生命周期清空，测试必须断言错误正文真实可见。滚动内容在状态栏安全区域之外裁剪，保留底部和键盘避让。
6. **完整滚动采集**：替代固定五次整屏滑动。使用真实运行时滚动容器的内容尺寸、偏移和可见安全视口记录几何；仅 DEBUG 隔离夹具允许增加不可见诊断。每次最多移动可见高度的 70%，保留至少 10% 的相邻覆盖，直到实测末尾；最多 60 张/状态，无法证明覆盖则失败而不是伪造成功。纳入两种卡片、账户/密码表单、首页词环与长文等实际可滚动视口；大字号词环只在环形区域响应旋转，页面边缘可以正常上下滚动。编辑器/聊天嵌套视口明确标记，键盘截图不代替完整长文保存测试。

实施 ownership：collection 工作者负责 Collection 下文件；account 工作者负责 Account 下界面及错误夹具交互（MiloApp 由主代理集成）；journey 工作者负责 RecallViews 的输入、聊天和日记；主代理负责 Home、JournalView 安全区域、DEBUG 滚动诊断、UITests、集成与记录。所有工作者不得回退其他人的编辑。

新增范围来自三份 review-iteration-1-{journey,account,collection}.json，均保留原始图片路径及哈希。第 2 次计划独立复核须审查本修订，不能沿用仅覆盖前两项的旧批准。

星球裁图片边的细小邻图残边、中文卡片短末行、动态效果和全部跨平台一致性继续记录待审，
不在缺少鸿蒙同状态原图时声称已经达到最终美学要求。

## 新验证条件

- 原有 9 条操作测试全部保留，不降低断言。
- 增加模板两按钮的顶部、宽度、高度与预览边界对齐、不得侵入标题行的实际界面几何断言。
- 最大字号长文实际输入后，编辑视口完整位于保存栏上方；独立审查截图确认末尾和光标可见。
- 重新预构建后执行测试和 53 状态采集；新图只关联新构建，不复用旧图作通过证据。
- 保留 31 项核心测试、26 项协调器检查、24 项证据验证器测试及框架自身检查。
- 所有缺失鸿蒙原图、完整滚动覆盖、真实账号接入和实体 iPhone 验证保持未完成。

## 阶段边界

先完成当前截图收集和独立检查，再复核本计划。只有明确新增的视觉修复及断言获独立复核后，
才为这一阶段建立新的有界运行，链接旧运行及失败图片；每项最多 3 次、无进展最多 2 次、
活跃执行时间最多 1 小时。新阶段的理由是实际审图暴露的具体问题和更精确的验证条件，
不能仅为了消除旧运行的阻塞或获得绿色结果而开始。最终双端验收规则保持不变。
